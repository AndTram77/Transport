-- CRM для таксопарка — начальная схема БД
-- Будет расширена согласно ТЗ

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Placeholder — основные таблицы будут добавлены после получения ТЗ
SELECT 'Transport CRM DB initialized' AS status;
